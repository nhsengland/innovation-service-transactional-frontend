import { FileUploadType } from '@app/base/forms';

export type DocumentFileStepInputType = {
  documentFile: FileUploadType | null;
};

export type DocumentFileStepOutputType = {
  documentFile: FileUploadType | null;
};
